<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Palindrome-express</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <main>
        <h1>Palindrome</h1>
  <section class ="container">
    <h4>Input a word to check if it is a palindrome.</h4>
  <form action="/checkWord" method="post">
        <label for="word"></label>
        <input type="text" placeholder="Ex. Anna" name="word">
        <button>Check</button>
    </form>

    <ul class="messages">
        <% for(var i=0; i<palindrome.length; i++) {%>
          <li class="message">
            <span class="bold"><%= palindrome[i].word %></span>
            <span><%= palindrome[i].outcome %></span>
          </li>
        <% } %>
        </ul>
  </section>
    </main>

    <script src="main.js"></script>
</body>
</html>
